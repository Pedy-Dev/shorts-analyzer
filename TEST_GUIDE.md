# 분석 결과 DB 저장 테스트 가이드

## 🚀 빠른 테스트 (브라우저 콘솔)

1. **http://localhost:3000** 접속 후 Google 로그인
2. **F12** 개발자 도구 열기 → **Console** 탭
3. **browser-test.js** 파일 내용 복사 → 콘솔에 붙여넣기 → Enter
4. 자동으로 테스트 실행되며 결과 확인

## 📝 수동 테스트 (실제 사용 시나리오)

### 1단계: 로그인 확인
- http://localhost:3000 접속
- 구글 로그인 버튼 클릭
- 로그인 완료 후 프로필 이미지 확인

### 2단계: 타 채널 분석 실행
1. "타 채널 분석" 탭 선택
2. YouTube API 키 입력
3. 채널 URL 입력 (예: https://www.youtube.com/@channel_name)
4. "분석 시작" 클릭
5. 분석 완료 대기 (약 30초~1분)

### 3단계: 저장 확인 (개발자 도구)
분석이 완료되면 개발자 도구 **Network** 탭에서:
- `save-analysis-history` 요청 찾기
- Status: **200 OK** 확인
- Response 탭에서 저장된 ID 확인

### 4단계: 분석 기록 탭에서 확인
1. "분석 기록" 탭 클릭
2. 방금 분석한 채널이 리스트에 나타나는지 확인
3. 해당 항목 클릭하여 상세 내용 표시 확인

## 🔍 DB 직접 확인 (Node.js)

```bash
# 터미널에서 실행
node test-db.js
```

출력 예시:
```
📌 데이터베이스 연결 테스트 시작...
✅ 저장된 분석 기록 수: 5
🔍 최근 분석 기록:
1. 채널명
   - ID: xxx
   - 생성일: 2024-11-20 15:30:00
```

## ⚠️ 문제 해결

### 분석은 되는데 저장이 안 되는 경우:
1. **콘솔 에러 확인**: F12 → Console
2. **500 에러**: API 서버 재시작 필요 (`npm run dev`)
3. **401 에러**: 다시 로그인

### 분석 기록이 안 보이는 경우:
1. 페이지 새로고침 (F5)
2. 로그인 상태 확인
3. Network 탭에서 `analysis-history` API 응답 확인

## 💡 디버깅 팁

콘솔에서 빠르게 확인:
```javascript
// 현재 로그인 상태
fetch('/api/user/me').then(r => r.json()).then(console.log)

// 저장된 기록 수
fetch('/api/analysis-history').then(r => r.json()).then(d => console.log('기록 수:', d.data?.length))

// 최근 기록 1개
fetch('/api/analysis-history').then(r => r.json()).then(d => console.log(d.data?.[0]))
```